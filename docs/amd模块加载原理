1、模块加载器内部初始化
    分析Script DOM data-main和data-baseurl属性，得到入口模块和模块基础路径
    设置模块加载器基础路径，如果存在data-baseurl属性，那么最终模块加载器基础路径为当前页面地址 + baseUrl

2、使用当前模块
    判断依赖模块是否加载完成
        否：
            1、创建未加载模块信息数组，包括模块名和模块路径
            2、设置未加载模块信息数组的属性指代当前模块的factory方法
            3、通过创建async异步加载script标签加载模块
                (1)、设置模块对象的url
                (2)、ie>8、ff、chrome使用onload和onerror事件绑定进行模块加载判断
                (3)、删除事件绑定和script DOM元素
                (4)、设置模块对象的status为加载成功状态符
                (5)、判断当前模块的依赖模块是否加载完成，如果加载完成调用当前模块的factory方法，没有的话返回第3步
        是：
            (1)、得到依赖模块的导出对象
            (2)、调用模块处理方法
    得到依赖模块的导出对象
    (1)、解析模块标识

        模块标识：模块文件相对路径 模块名需和文件名保持一致

